<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/explain_frag"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".ExplainFragment"
    android:paddingHorizontal="20dp"
    >

    <!-- TODO: Update blank fragment layout -->
    <TextView
        android:id="@+id/word_target_ex"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="a"
        android:textSize="26dp"
        android:textColor="#444"
        />

    <TextView
        android:id="@+id/word_pronun_ex"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="/ei, ə/"
        android:textSize="18dp"
        android:textColor="#00f"
       />

    <ScrollView
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:scrollbars="none"
        android:fillViewport="true"
        android:layout_marginTop="20dp">

        <TextView
            android:id="@+id/word_explain_ex"
            android:layout_width="wrap_content"
            android:layout_height="fill_parent"
            android:justificationMode="inter_word"
            android:text="*  danh từ,  số nhiều as,  a'sAfter downloading an Android keyboard—or even more than one—launch it to complete the installation. Most apps walk you through the process of enabling a keyboard and setting it as the default, but it's also easy to do so manually.*  danh từ,  số nhiều as,  a'sAfter downloading an Android keyboard—or even more than one—launch it to complete the installation. Most apps walk you through the process of enabling a keyboard and setting it as the default, but it's also easy to do so manually.*  danh từ,  số nhiều as,  a'sAfter downloading an Android keyboard—or even more than one—launch it to complete the installation. Most apps walk you through the process of enabling a keyboard and setting it as the default, but it's also easy to do so manually.*  danh từ,  số nhiều as,  a'sAfter downloading an Android keyboard—or even more than one—launch it to complete the installation. Most apps walk you through the process of enabling a keyboard and setting it as the default, but it's also easy to do so manually.*  danh từ,  số nhiều as,  a'sAfter downloading an Android keyboard—or even more than one—launch it to complete the installation. Most apps walk you through the process of enabling a keyboard and setting it as the default, but it's also easy to do so manually.*  danh từ,  số nhiều as,  a'sAfter downloading an Android keyboard—or even more than one—launch it to complete the installation. Most apps walk you through the process of enabling a keyboard and setting it as the default, but it's also easy to do so manually.*  danh từ,  số nhiều as,  a'sAfter downloading an Android keyboard—or even more than one—launch it to complete the installation. Most apps walk you through the process of enabling a keyboard and setting it as the default, but it's also easy to do so manually.*  danh từ,  số nhiều as,  a'sAfter downloading an Android keyboard—or even more than one—launch it to complete the installation. Most apps walk you through the process of enabling a keyboard and setting it as the default, but it's also easy to do so manually.*  danh từ,  số nhiều as,  a'sAfter downloading an Android keyboard—or even more than one—launch it to complete the installation. Most apps walk you through the process of enabling a keyboard and setting it as the default, but it's also easy to do so manually."
            android:textColor="#444"
            android:textSize="20dp" />
    </ScrollView>
</LinearLayout>